export function fetchStartingLocation(input) {
  console.log(input)
    // return (dispatch) => {
    //   dispatch({ type: 'FETCHING_SUGGESTED_START_LOCATIONS' });
    //   return fetch(`/api/search_start/${input}`)
    //     .then(response => response.json())
    //     .then(locations => dispatch({ type: 'DISPLAY_START_LOCATIONS', locations }));
    // };
  }